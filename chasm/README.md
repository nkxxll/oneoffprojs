# react

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run src/index.tsx
```

This project was created using `bun create tui`. [create-tui](https://git.new/create-tui) is the easiest way to get started with OpenTUI.

## Todo

- [ ] q in the command palette quits which should not happen
- [ ] maybe dont use q to quit at all just esc and ctrl-[
- [ ] fast should execute async it does not have to block the ui
